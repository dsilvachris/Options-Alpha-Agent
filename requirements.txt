# Core
mcp>=1.2.0            # official MCP Python SDK — stdio client for alpaca-mcp-server
uv>=0.5.0             # provides `uvx` used to launch the Alpaca MCP server
python-dotenv>=1.0.0  # .env loading

# Dashboard
fastapi>=0.115.0
uvicorn>=0.32.0
